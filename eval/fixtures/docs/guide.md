# Platform policy

This project supports desktop Linux, Windows, and macOS. Android and iOS clients
are outside the project's scope.

# Privacy

The client runs locally. It has no telemetry and no hosted backend.

# Tool API

Define `execute` on your tool class to implement its behavior.
# Message forwarding

Right-click the message or picture, choose Forward, then select the destination chat.
